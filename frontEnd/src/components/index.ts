/**
 * 这个文件作为组件的目录
 * 目的是统一管理对外输出的组件，方便分类
 */

import Footer from './Footer';
import Navigation from './Navigation';
import MainLayout from './Layout';
import UserInfo from './UserInfo';

export { Footer, Navigation, MainLayout, UserInfo };
